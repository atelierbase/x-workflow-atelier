# 画像リクエスト — X（@AtelierBase_own）

> クラウドの補充ルーチンが「画像付き投稿」を作ったとき、ここに指示を追記する。
> オーナー（私）が ChatGPT(image) で生成 → `x/storage/images/[投稿ID].png` に保存 → commit & push。
> 配信（GitHub Actions）時に画像があれば自動添付、無ければ本文のみでフォールバック配信。

---

<!-- 以下にルーチンが追記する。処理済みは見出しに ✅ を付けて残す -->
